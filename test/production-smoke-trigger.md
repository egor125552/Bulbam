# Production messaging smoke trigger

This file intentionally lives under `test/` so a small auditable main-branch commit can trigger the production messaging smoke workflow after Phase 2 infrastructure or workflow changes.
